[project]
name = "py-lambda-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "temporalio[lambda-worker-otel]>=1.26.0",
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["*.py"]
